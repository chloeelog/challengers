const colorScheme = {
  primary: "rgb(255, 66, 67)", // "#ff4243"
  gray: "rgb(165, 156, 156)", // "#a59c9c"
  black: "rgb(26, 0, 0)", // "#1a0000"
  grayTransparent: "rgba(51, 0, 0, 0.3)"
}

const fontScheme = {
  extraLarge: "32px",
  large: "24px",
  medium: "16px",
  small: "12px",
}

const iconScheme = {
  bottomTabIconSize: 30,
  tabHeaderIconSize: 20,
}

export const theme = {
  colorScheme,
  fontScheme,
  iconScheme,
}